process {
    withName: '.*IRODS_FIND' {
        cpus = 1
        memory = 2.GB
        queue = 'normal'
        errorStrategy = { task.exitStatus == 3 ? "terminate" : 'retry' }
        maxForks = 10
    }
}