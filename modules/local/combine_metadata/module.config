process {
    withName: '.*COMBINE_METADATA.*' {
        queue = 'normal'
        cpus = 2
        memory = 4.GB
        debug = true
        publishDir = [
            mode: params.publish_mode,
            path: "${params.publish_dir}"
        ]
    }
}