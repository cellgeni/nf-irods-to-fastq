process {
    if (params.crams || params.cram2fastq) {
        withName: '.*IRODS_GETFILE' {
            ext.args = "-KV -X restart.txt --retries 3"
            array = 500
            cpus = 1
            memory = 2.GB
            queue = 'transfer'
            maxForks = 20
        }
    }
}
