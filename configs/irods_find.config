process {
    withName: 'IRODS_FINDCRAMS:IRODS_FIND' {
        ext.args = "-z ${params.irods_zone} -d target = 1 and type != fastq and"
        queue = 'normal'
        errorStrategy = { task.exitStatus == 3 ? "terminate" : 'retry' }
        maxForks = 10
    }
}